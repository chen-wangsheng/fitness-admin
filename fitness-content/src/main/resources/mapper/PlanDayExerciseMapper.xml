<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fitness.admin.content.mapper.PlanDayExerciseMapper">

    <select id="selectByPlanDayId" resultType="com.fitness.admin.content.vo.PlanExerciseVO">
        SELECT pde.*, e.name as exerciseName FROM plan_day_exercise pde
        LEFT JOIN exercise e ON pde.exercise_id = e.id
        WHERE pde.plan_day_id = #{planDayId} ORDER BY pde.sort
    </select>

</mapper>
